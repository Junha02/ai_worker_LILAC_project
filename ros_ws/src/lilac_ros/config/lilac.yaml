language_manager_node:
  ros__parameters:
    initial_instruction: "Pick up the cup and pour water into the bowl."

latent_input_node:
  ros__parameters:
    publish_hz: 30.0
    axis_lr: 0
    axis_ud: 1
    deadzone: 0.15
    button_reset: 0
    button_pop: 1

lilac_policy_node:
  ros__parameters:
    publish_hz: 30.0
    action_pos_scale: 0.01
    action_rot_scale: 0.04
    demo_fallback_enabled: true

mujoco_sim_node:
  ros__parameters:
    publish_hz: 30.0
